<%- include('../partials/header') %>

<link rel="stylesheet" href="/css/dashboard.css">

    <div class="hero-section">
        <div class="absolute inset-0 overflow-hidden pointer-events-none">
            <div class="absolute top-20 left-8 floating-card-left-1 animate-float-slow">
                <img src="/images/img1.jpg" alt="Event 1" class="w-full h-full object-cover rounded-2xl shadow-2xl">
            </div>
            <div class="absolute top-1/3 left-4 floating-card-left-2 animate-float-medium">
                <img src="/images/img2.jpg" alt="Event 2" class="w-full h-full object-cover rounded-2xl shadow-xl">
            </div>
            <div class="absolute bottom-20 left-12 floating-card-left-3 animate-float-fast">
                <img src="/images/img3.jpg" alt="Event 3" class="w-full h-full object-cover rounded-3xl shadow-lg">
            </div>
            
            <div class="absolute top-20 right-8 floating-card-right-1 animate-float-slow-reverse">
                <img src="/images/img4.jpg" alt="Event 4" class="w-full h-full object-cover rounded-3xl shadow-2xl">
            </div>
            <div class="absolute top-1/3 right-4 floating-card-right-2 animate-float-medium-reverse">
                <img src="/images/img5.jpg" alt="Event 5" class="w-full h-full object-cover rounded-3xl shadow-xl">
            </div>
            <div class="absolute bottom-20 right-12 floating-card-right-3 animate-float-fast-reverse">
                <img src="/images/img8.jpg" alt="Event 6" class="w-full h-full object-cover rounded-3xl shadow-lg">
            </div>
        </div>
        
        <div class="relative z-10 px-6 py-24 sm:py-32 lg:py-40">
            <div class="max-w-4xl mx-auto text-center">
                <h1 class="text-4xl sm:text-5xl lg:text-6xl font-bold mb-4 text-gray-800 animate-fade-in">
                    Discover Amazing Events
                </h1>
                <p class="text-lg sm:text-xl lg:text-2xl mb-8 text-gray-600 max-w-3xl mx-auto animate-fade-in-delay">
                    Explore exciting campus events, seminars, workshops, and cultural activities at NLU
                </p>
                
                <div class="max-w-2xl mx-auto mb-8 animate-slide-up">
                    <div class="search-container">
                        <div class="search-input-wrapper">
                            <i class="fas fa-search search-icon"></i>
                            <input 
                                type="text" 
                                id="heroSearch"
                                placeholder="Search events by name, category, or date..."
                                class="search-input"
                                onkeyup="handleHeroSearch(event)"
                            >
                        </div>
                        <button 
                            onclick="performHeroSearch()"
                            class="search-button"
                        >
                            <i class="fas fa-search mr-2"></i>
                            Search Events
                        </button>
                    </div>
                </div>
                
                <div class="animate-fade-in-delay-2">
                    <button 
                        onclick="scrollToEvents()"
                        class="bg-white hover:bg-[#0ea5a4] text-[#0ea5a4] hover:text-white px-4 py-2 rounded-full text-base font-bold transition-all duration-300 hover:scale-105 hover:shadow-xl focus:outline-none focus:ring-2 focus:ring-[#0ea5a4]/50 border-2 border-[#0ea5a4]"
                    >
                        <i class="fas fa-arrow-down mr-2"></i>
                        Get Started
                    </button>
                </div>
            </div>
        </div>
    </div>

<div class="dashboard-container">
    <%- include('../partials/sidebar') %>
    
    <main class="flex-1">
        <div class="p-4 sm:p-6">
            <h2 class="responsive-text-3xl font-bold mb-2 text-gray-800">Welcome back, <%= currentUser.profile.fullName %>!</h2>
            <p class="text-gray-700 sm:text-gray-600 text-base sm:text-lg mb-6 sm:mb-8">Here's your personalized dashboard overview</p>

            <div class="bg-white rounded-lg shadow-sm border border-gray-200 p-3 sm:p-3.5 mb-4">
                <div class="flex items-center justify-between mb-2">
                    <h2 class="text-xs font-bold text-gray-800 flex items-center">
                        <i class="fas fa-broadcast-tower text-gray-400 mr-1.5 text-[10px]"></i>Live Updates
                    </h2>
                    <span class="text-[10px] text-gray-400">Auto: 30s</span>
                </div>
                <div class="grid grid-cols-1 sm:grid-cols-2 gap-2">
                    <div class="border border-gray-100 rounded-md p-2.5">
                        <h3 class="text-[11px] font-semibold text-gray-500 mb-1.5 flex items-center">
                            <i class="fas fa-flag text-gray-300 mr-1.5 text-[9px]"></i>Match Updates
                        </h3>
                        <div id="matchUpdatesContainer" class="space-y-1.5">
                            <p class="text-gray-300 text-[10px] text-center py-2">Loading...</p>
                        </div>
                    </div>
                    <div class="border border-gray-100 rounded-md p-2.5">
                        <h3 class="text-[11px] font-semibold text-gray-500 mb-1.5 flex items-center">
                            <i class="fas fa-trophy text-gray-300 mr-1.5 text-[9px]"></i>Winners
                        </h3>
                        <div id="winnerUpdatesContainer" class="space-y-1.5">
                            <p class="text-gray-300 text-[10px] text-center py-2">Loading...</p>
                        </div>
                    </div>
                </div>
            </div>

            <div class="flex flex-wrap gap-2 mb-6">
                <button onclick="filterEvents('all')" class="category-btn active px-4 py-2 rounded-full text-sm font-medium transition-colors bg-[#0ea5a4] text-white border border-[#0ea5a4]">
                    All Events
                </button>
                <button onclick="filterEvents('seminar')" class="category-btn px-4 py-2 rounded-full text-sm font-medium transition-colors bg-gray-200 text-gray-700 hover:bg-gray-300 border border-[#0ea5a4] hover:bg-[#0ea5a4] hover:text-white">
                    Seminars
                </button>
                <button onclick="filterEvents('workshop')" class="category-btn px-4 py-2 rounded-full text-sm font-medium transition-colors bg-gray-200 text-gray-700 hover:bg-gray-300 border border-[#0ea5a4] hover:bg-[#0ea5a4] hover:text-white">
                    Workshops
                </button>
                <button onclick="filterEvents('hackathon')" class="category-btn px-4 py-2 rounded-full text-sm font-medium transition-colors bg-gray-200 text-gray-700 hover:bg-gray-300 border border-[#0ea5a4] hover:bg-[#0ea5a4] hover:text-white">
                    Hackathons
                </button>
                <button onclick="filterEvents('quiz')" class="category-btn px-4 py-2 rounded-full text-sm font-medium transition-colors bg-gray-200 text-gray-700 hover:bg-gray-300 border border-[#0ea5a4] hover:bg-[#0ea5a4] hover:text-white">
                    Quizzes
                </button>
                <button onclick="filterEvents('talkshow')" class="category-btn px-4 py-2 rounded-full text-sm font-medium transition-colors bg-gray-200 text-gray-700 hover:bg-gray-300 border border-[#0ea5a4] hover:bg-[#0ea5a4] hover:text-white">
                    Talk Shows
                </button>
                <button onclick="filterEvents('bootcamp')" class="category-btn px-4 py-2 rounded-full text-sm font-medium transition-colors bg-gray-200 text-gray-700 hover:bg-gray-300 border border-[#0ea5a4] hover:bg-[#0ea5a4] hover:text-white">
                    Bootcamps
                </button>
                <button onclick="filterEvents('cultural')" class="category-btn px-4 py-2 rounded-full text-sm font-medium transition-colors bg-gray-200 text-gray-700 hover:bg-gray-300 border border-[#0ea5a4] hover:bg-[#0ea5a4] hover:text-white">
                    Cultural
                </button>
                <button onclick="filterEvents('sports')" class="category-btn px-4 py-2 rounded-full text-sm font-medium transition-colors bg-gray-200 text-gray-700 hover:bg-gray-300 border border-[#0ea5a4] hover:bg-[#0ea5a4] hover:text-white">
                    Sports
                </button>
            </div>

            <div class="flex flex-col sm:flex-row justify-between items-start sm:items-center mb-6 space-y-4 sm:space-y-0">
                <h2 class="responsive-text-2xl font-bold text-gray-800 flex items-center">
                    Available <span id="category-title">ALL</span> Events
                </h2>
                <div class="flex items-center space-x-2 sm:space-x-4">
                    <span class="text-xs sm:text-sm text-gray-500">Total Events</span>
                    <span id="event-count" class="text-lg sm:text-xl font-bold text-blue-600"><%= allEvents.length %></span>
                </div>
            </div>
            
            <% if (allEvents && allEvents.length > 0) { %>
                <div class="events-grid">
                        <% allEvents.forEach((event, index) => { %>
                            <div class="event-card group" data-category="<%= event.category || 'other' %>">
                                <div class="event-image">
                                    <% if (event.poster && event.poster.trim() !== '') { %>
                                        <img src="<%= event.poster %>" 
                                             alt="<%= event.title %>" 
                                             class="w-full h-full object-cover"
                                             style="object-fit: cover;">
                                    <% } else { %>
                                        <div class="w-full h-full flex items-center justify-center bg-gradient-to-br from-blue-100 to-purple-100">
                                            <div class="text-center">
                                                <i class="fas fa-calendar-alt text-4xl text-blue-600 mb-2"></i>
                                                <p class="text-blue-600 text-sm">No poster uploaded</p>
                                            </div>
                                        </div>
                                    <% } %>
                                </div>
                                
                                <div class="event-content">
                                    <div class="event-title-row">
                                        <h3 class="event-title">
                                            <%= event.title %>
                                        </h3>
                                        <% if (event.category) { %>
                                            <span class="category-badge flex-shrink-0
                                                <% if (event.category === 'sports') { %>
                                                    bg-green-100 text-green-800
                                                <% } else if (event.category === 'workshop') { %>
                                                    bg-blue-100 text-blue-800
                                                <% } else if (event.category === 'cultural') { %>
                                                    bg-purple-100 text-purple-800
                                                <% } else if (event.category === 'seminar') { %>
                                                    bg-yellow-100 text-yellow-800
                                                <% } else { %>
                                                    bg-gray-100 text-gray-800
                                                <% } %>
                                            ">
                                                <%= event.category.charAt(0).toUpperCase() + event.category.slice(1) %>
                                            </span>
                                        <% } %>
                                        <% if (event.eventMode) { %>
                                            <span class="category-badge flex-shrink-0 <%= event.eventMode === 'versity' ? 'bg-red-100 text-red-800' : 'bg-teal-100 text-teal-800' %> ml-1">
                                                <%= event.eventMode === 'versity' ? 'Versity Week' : 'Normal Day' %>
                                            </span>
                                        <% } %>
                                    </div>
                                    
                                    <p class="event-description mb-3">
                                        <%= event.description || 'No description available' %>
                                    </p>
                                    
                                    <div class="event-details mb-4">
                                        <div class="detail-row">
                                            <div class="detail-item">
                                                <i class="fas fa-calendar text-gray-400"></i>
                                                <span><%= new Date(event.date).toLocaleDateString('en-US', { weekday: 'short', month: 'short', day: 'numeric' }) %></span>
                                            </div>
                                            <div class="detail-item">
                                                <i class="fas fa-clock text-gray-400"></i>
                                                <span><%= event.startTime %> - <%= event.endTime %></span>
                                            </div>
                                        </div>
                                        <div class="detail-row full-width">
                                            <div class="detail-item">
                                                <i class="fas fa-user text-gray-400"></i>
                                                <span><b>Organized By</b> <%= event.organizer.profile.firstName %> <%= event.organizer.profile.lastName %></span>
                                            </div>
                                        </div>
                                    </div>
                                    
                                    <div class="flex justify-end">
                                        <a href="/student/events/<%= event._id %>" class="view-details-btn">
                                            View Details
                                            <i class="fas fa-arrow-right ml-2"></i>
                                        </a>
                                    </div>
                                </div>
                            </div>
                        <% }) %>
                    </div>
                <% } else { %>
                    <div class="no-events">
                        <i class="fas fa-calendar-times text-gray-300 text-6xl mb-4"></i>
                        <h3 class="text-xl font-semibold text-gray-600 mb-2">No Events Available</h3>
                        <p class="text-gray-500">There are currently no events available. Check back later for new events!</p>
                    </div>
                <% } %>
        </div>
    </main>
</div>

<script>
function handleHeroSearch(event) {
    if (event.key === 'Enter') {
        performHeroSearch();
    }
}

function performHeroSearch() {
    const searchInput = document.getElementById('heroSearch');
    const searchTerm = searchInput.value.trim();
    
    if (searchTerm) {
        if (typeof searchEventsOnDashboard === 'function') {
            searchEventsOnDashboard(searchTerm);
        }
        
        scrollToEvents();
    }
}

function scrollToEvents() {
    const eventsSection = document.querySelector('.p-4.sm\\:p-6');
    if (eventsSection) {
        eventsSection.scrollIntoView({ 
            behavior: 'smooth',
            block: 'start'
        });
    }
}

function filterEvents(category) {
    const eventCards = document.querySelectorAll('.event-card');
    const categoryButtons = document.querySelectorAll('.category-btn');
    const categoryTitle = document.getElementById('category-title');
    const eventCount = document.getElementById('event-count');
    
    categoryButtons.forEach(btn => {
        btn.classList.remove('bg-[#0ea5a4]', 'text-white', 'active');
        btn.classList.add('bg-gray-200', 'text-gray-700');
        
        btn.style.opacity = '0.7';
        btn.style.transform = 'scale(0.95)';
        btn.style.color = '#000000';
        btn.style.boxShadow = 'none';
        btn.style.backgroundColor = '#f3f4f6';
    });
    
    let activeButton = null;
    
    categoryButtons.forEach(btn => {
        const onclickAttr = btn.getAttribute('onclick');
        if (onclickAttr && onclickAttr.includes(`filterEvents('${category}')`)) {
            activeButton = btn;
            return false;        }
    });
    
    if (activeButton) {
        activeButton.classList.remove('bg-gray-200', 'text-gray-700');
        
        activeButton.classList.add('bg-[#0ea5a4]', 'text-white', 'active');
        
        activeButton.style.opacity = '1';
        activeButton.style.transform = 'scale(1)';
        activeButton.style.color = '#ffffff';
        activeButton.style.boxShadow = '0 4px 12px rgba(14, 165, 164, 0.3)';
        activeButton.style.backgroundColor = '#0ea5a4';
        
        if (window.innerWidth <= 640) {
            activeButton.style.backgroundColor = '#0ea5a4';
            activeButton.style.color = '#ffffff';
        }
    }
    
    if (category === 'all') {
        categoryTitle.textContent = 'ALL';
    } else {
        categoryTitle.textContent = category.charAt(0).toUpperCase() + category.slice(1);
    }
    
    let visibleCount = 0;
    eventCards.forEach(card => {
        const eventCategory = card.getAttribute('data-category');
        
        if (category === 'all' || eventCategory === category) {
            card.style.display = 'block';
            visibleCount++;
        } else {
            card.style.display = 'none';
        }
    });
    
    eventCount.textContent = visibleCount;
    
    const eventsGrid = document.querySelector('.events-grid');
    const noEventsMessage = document.querySelector('.no-events');
    
    if (visibleCount === 0 && eventsGrid) {
        if (noEventsMessage) {
            noEventsMessage.style.display = 'block';
            noEventsMessage.innerHTML = `
                <i class="fas fa-calendar-times text-gray-300 text-6xl mb-4"></i>
                <h3 class="text-xl font-semibold text-gray-600 mb-2">No ${category === 'all' ? '' : category.charAt(0).toUpperCase() + category.slice(1) + ' '}Events Available</h3>
                <p class="text-gray-500">There are currently no ${category === 'all' ? '' : category + ' '}events available. Check back later for new events!</p>
            `;
        }
    } else if (noEventsMessage) {
        noEventsMessage.style.display = 'none';
    }
}

function searchEventsOnDashboard(searchTerm) {
    const eventCards = document.querySelectorAll('.event-card');
    const eventCount = document.getElementById('event-count');
    const categoryTitle = document.getElementById('category-title');
    const noEventsMessage = document.querySelector('.no-events');
    const allEventsBtn = document.querySelector('.category-btn[onclick="filterEvents(\'all\')"]');
    
    const categoryButtons = document.querySelectorAll('.category-btn');
    categoryButtons.forEach(btn => {
        btn.classList.remove('active', 'bg-[#0ea5a4]', 'text-white');
    });
    
    if (allEventsBtn) {
        allEventsBtn.classList.add('active', 'bg-[#0ea5a4]', 'text-white');
    }
    
    categoryTitle.textContent = 'ALL';
    
    if (!searchTerm) {
        eventCards.forEach(card => {
            card.style.display = 'block';
        });
        
        const visibleCards = Array.from(eventCards).filter(card => card.style.display !== 'none');
        eventCount.textContent = visibleCards.length;
        
        if (noEventsMessage) {
            noEventsMessage.style.display = 'none';
        }
        return;
    }
    
    const searchLower = searchTerm.toLowerCase();
    let visibleCount = 0;
    
    eventCards.forEach(card => {
        const title = card.querySelector('.event-title')?.textContent.toLowerCase() || '';
        const description = card.querySelector('.event-description')?.textContent.toLowerCase() || '';
        const venue = card.querySelector('.event-venue')?.textContent.toLowerCase() || '';
        const category = card.getAttribute('data-category')?.toLowerCase() || '';
        
        if (title.includes(searchLower) || 
            description.includes(searchLower) || 
            venue.includes(searchLower) || 
            category.includes(searchLower)) {
            card.style.display = 'block';
            visibleCount++;
        } else {
            card.style.display = 'none';
        }
    });
    
    eventCount.textContent = visibleCount;
    
    if (visibleCount === 0) {
        if (noEventsMessage) {
            noEventsMessage.style.display = 'block';
            noEventsMessage.innerHTML = `
                <i class="fas fa-search text-gray-300 text-6xl mb-4"></i>
                <h3 class="text-xl font-semibold text-gray-600 mb-2">No events found matching your search.</h3>
                <p class="text-gray-500">Try searching with different keywords or check back later for new events!</p>
            `;
        }
    } else if (noEventsMessage) {
        noEventsMessage.style.display = 'none';
    }
}

document.addEventListener('DOMContentLoaded', function() {
    const urlParams = new URLSearchParams(window.location.search);
    const searchParam = urlParams.get('search');
    
    if (searchParam) {
        searchEventsOnDashboard(searchParam);
    }
});

window.searchEventsOnDashboard = searchEventsOnDashboard;

async function loadLiveUpdates() {
    try {
        const response = await fetch('/student/notifications');
        const result = await response.json();
        
        const matchContainer = document.getElementById('matchUpdatesContainer');
        const winnerContainer = document.getElementById('winnerUpdatesContainer');
        if (!matchContainer || !winnerContainer) return;
        
        if (result.success && result.notifications.length > 0) {
            const matchNotifications = result.notifications.filter(n => n.type === 'next_match' || n.type === 'match_started')
                .sort((a, b) => {
                    const order = { match_started: 0, next_match: 1 };
                    return (order[a.type] ?? 2) - (order[b.type] ?? 2);
                }).slice(0, 3);
            const winnerNotifications = result.notifications.filter(n => n.type === 'winner_declared').slice(0, 3);
            
            if (matchNotifications.length > 0) {
                matchContainer.innerHTML = matchNotifications.map(n => {
                    let statusColor, statusLabel, statusIcon;
                    if (n.type === 'next_match') {
                        statusColor = 'text-purple-500';
                        statusLabel = 'Next Match';
                        statusIcon = 'fa-clock';
                    } else {
                        statusColor = 'text-blue-600';
                        statusLabel = 'Match Started';
                        statusIcon = 'fa-play-circle';
                    }
                    
                    return `
                        <div class="border border-gray-100 rounded-md p-2 hover:shadow-sm transition-shadow">
                            <div class="flex items-center justify-between mb-1">
                                <span class="inline-flex items-center gap-1 text-[10px] font-semibold ${statusColor}">
                                    <i class="fas ${statusIcon} text-[8px]"></i>${statusLabel}
                                </span>
                                <span class="text-[9px] text-gray-400">${new Date(n.createdAt).toLocaleTimeString([], {hour: '2-digit', minute:'2-digit'})}</span>
                            </div>
                            <p class="text-[11px] font-semibold text-gray-800 mb-0.5">${n.event?.title || 'Event'}</p>
                            <div class="flex items-center gap-1.5 text-[10px]">
                                <span class="font-medium text-gray-600">${n.teamA || 'TBD'}</span>
                                <span class="text-gray-300 font-bold">VS</span>
                                <span class="font-medium text-gray-600">${n.teamB || 'TBD'}</span>
                            </div>
                            ${n.message ? `<p class="text-[9px] text-gray-400 mt-0.5 italic">${n.message}</p>` : ''}
                        </div>
                    `;
                }).join('');
            } else {
                matchContainer.innerHTML = '<p class="text-gray-300 text-[10px] text-center py-2">No match updates</p>';
            }
            
            if (winnerNotifications.length > 0) {
                winnerContainer.innerHTML = winnerNotifications.map(n => {
                    const winnerName = n.winner || n.message || 'TBD';
                    
                    return `
                        <div class="border border-gray-100 rounded-md p-2 hover:shadow-sm transition-shadow">
                            <div class="flex items-center justify-between mb-1">
                                <span class="inline-flex items-center gap-1 text-[10px] font-semibold text-green-600">
                                    <i class="fas fa-trophy text-[8px]"></i>Winner Announced
                                </span>
                                <span class="text-[9px] text-gray-400">${new Date(n.createdAt).toLocaleTimeString([], {hour: '2-digit', minute:'2-digit'})}</span>
                            </div>
                            <p class="text-[11px] font-semibold text-gray-800 mb-0.5">${n.event?.title || 'Event'}</p>
                            <p class="text-[10px] text-gray-500 mb-0.5">${n.teamA || ''} vs ${n.teamB || ''}</p>
                            <p class="text-[11px] font-bold text-blue-600">${winnerName}</p>
                        </div>
                    `;
                }).join('');
            } else {
                winnerContainer.innerHTML = '<p class="text-gray-300 text-[10px] text-center py-2">No winners announced yet</p>';
            }
        } else {
            matchContainer.innerHTML = '<p class="text-gray-300 text-[10px] text-center py-2">No live updates yet</p>';
            winnerContainer.innerHTML = '<p class="text-gray-300 text-[10px] text-center py-2">No live updates yet</p>';
        }
    } catch (error) {
        console.error('Error loading live updates:', error);
    }
}

document.addEventListener('DOMContentLoaded', function() {
    loadLiveUpdates();
    setInterval(loadLiveUpdates, 30000);});
</script>

<%- include('../partials/footer') %>
